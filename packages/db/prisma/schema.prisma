generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  name      String?
  email     String   @unique
  password  String?
  createdAt DateTime @default(now())
  sites     Site[]

  // Add these 2 relations for OAuth + Sessions
  accounts Account[]
  sessions Session[]
}

model Account {
  id                String @id @default(uuid())
  userId            String
  type              String
  provider          String
  providerAccountId String

  refresh_token String?
  access_token  String?
  expires_at    Int?
  token_type    String?
  scope         String?
  id_token      String?
  session_state String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(uuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@id([identifier, token])
}

model Site {
  id        String   @id @default(uuid())
  userId    String
  domain    String   @unique
  createdAt DateTime @default(now())

  user       User        @relation(fields: [userId], references: [id])
  dailyStats DailyStat[]
}

/*model DailyStat {
  id           String   @id @default(uuid())
  siteId       String
  eventType    String?
  date         DateTime
  previousPage String? //referrer: document.referrer 
  page         String //pathname: window.location.pathname
  pageTitle    String?
  
  country      String?
  browser      String?
  device       String?
  os           String?
  TimeSpent    Int? //timeSpent: custom implementation
  
  IpAddress    String? //ipAddress: custom implementation

  site Site @relation(fields: [siteId], references: [id])
}*/

model DailyStat {
  id           String   @id @default(uuid())
  siteId       String
  visitorId    String   // ✅ new
  eventType    String?
  date         DateTime
  previousPage String?
  page         String
  pageTitle    String?
  country      String?
  browser      String?
  device       String?
  os           String?
  TimeSpent    Int?
  IpAddress    String?
  site Site @relation(fields: [siteId], references: [id])
}

